# antislop audit 001 — 2026-09-16

Mode: AFTER (audit terhadap situs yang sudah ada). Dipicu oleh permintaan langsung user
("audit mulai di semua halaman, mulai dari beranda"). Baru sempat mengaudit **beranda
(`index.html`)** secara menyeluruh; halaman lain (29 file HTML lainnya) belum diaudit satu
per satu, kecuali untuk pola em dash di bawah yang scope-nya sudah dicek ke semua file.

## Temuan #1 — HIGH (R-02, Hard Gate) — FIXED

**Em dash (`&mdash;`) dipakai di teks yang tampil ke pengunjung, ditemukan di 31 dari 31 file
HTML** (semua halaman di root proyek, index-card.html termasuk). Bukan di komentar developer
— ini di teks yang benar-benar render: judul meta og/twitter, badge foto stok/ilustrasi,
jawaban FAQ, disclaimer ulasan, catatan sumber regulasi, dll.

Contoh (index.html sebelum fix): baris 9, 13, 79, 86, 260, 266, 333, 442, 452, 462.

**Fix:** setiap kemunculan dibaca satu per satu dan diganti dengan tanda baca alami sesuai
konteks kalimat (bukan cari-ganti mekanis satu pola):
- Pola judul meta (`"<Judul Halaman> — RSUD Morowali"`) → diganti tanda hubung `-`, supaya
  konsisten dengan `<title>` tag yang sudah lebih dulu pakai tanda hubung (lihat commit
  `a9cb978`).
- Pola badge foto stok/ilustrasi → diganti koma.
- Pola kalimat penjelas ("Bisa — ...", "Sudah — ...", dll) → dipecah jadi 2 kalimat dengan titik.
- Detail lengkap tiap kasus ada di riwayat commit.

Status: **selesai, dikonfirmasi 0 kemunculan `&mdash;` tersisa** di seluruh file `*.html`.

## Cakupan yang belum diaudit

Baru `index.html` yang benar-benar dibaca section-per-section terhadap seluruh Delivery Gate
(gradient/glow/badge/CTA/FAQ/dll). 29 halaman lain belum dicek selain untuk pola em dash.
Beranda sendiri hasilnya bersih: reviews/berita/foto stok sudah ditandai jujur sebagai
placeholder (R-18/R-38), FAQ spesifik produk (R-28), navbar semua valid (R-24), gradient/glow
hero sudah beralasan & sesuai dial ENERGY 2 di `DESIGN.md` (R-01/R-13).

Belum dicek karena keterbatasan environment: kontras warna otomatis (plugin
`antislop-contrast` gagal connect, Node tidak ada di PATH) — dicek manual dengan membaca nilai
warna CSS untuk elemen yang terlihat berisiko (hero-slide-tag, news-card-tag), tidak ada yang
mencurigakan, tapi belum diverifikasi dengan rasio kontras pasti.

Lanjutkan ke halaman berikutnya (profil.html dst, urutan nav header) bila diminta.
